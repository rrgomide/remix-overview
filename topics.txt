Topics
======
(x) Show general structure
(x) Show root route
(x) Show index route
(x) /crud
  (x) Show current code
  (x) Show problems
    (x) Disable event.preventDefault()
    (x) Force delay
    (x) Show Race Conditions by clicking fast on "Learn" Toggle
    (x) Disable JavaScript and reload page
(x) /defer
  (x) Show current code and problems (too slow)

Remix
=====
  () /crud
    () Add loader and action
    () Add <Form> for CRUD operations
    () Add Optimistic UI with useFetcher
    () Disable JavaScript and see what still works

  () /defer
    () Use defer to mitigate the slow responses
    () Show differences in calling order

() Show "/thanks" route
